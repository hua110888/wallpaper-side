{
  "name": "wallpaper-static-netlify-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static Netlify deployment with Netlify Database migrations.",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\""
  }
}
